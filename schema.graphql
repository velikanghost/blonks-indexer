type Blonks_Approval {
  id: ID!
  owner: String!
  approved: String!
  tokenId: BigInt!
}

type Blonks_ApprovalForAll {
  id: ID!
  owner: String!
  operator: String!
  approved: Boolean!
}

type Blonks_OwnershipTransferred {
  id: ID!
  previousOwner: String!
  newOwner: String!
}

type Blonks_Transfer {
  id: ID!
  from: String!
  to: String!
  tokenId: BigInt!
}

